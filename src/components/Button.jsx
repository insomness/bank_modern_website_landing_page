const Button = ({styles}) => {
  return (
   <button type='button' className={`py-4 px- font-poppins font-medium text-[18px] leading-[27px] capitalize h-[64px] w-[170px] bg-blue-gradient rounded-[10px] ${styles}`}>Get Started</button>
  )
}

export default Button